name: ghar_ka_mali_customer
description: Ghar Ka Mali - Customer App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  http: ^1.2.0
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  intl: ^0.19.0
  pinput: ^3.0.1
  flutter_rating_bar: ^4.0.1
  image_picker: ^1.0.7
  permission_handler: ^11.2.0
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  url_launcher: ^6.2.4
  socket_io_client: ^2.0.3+1
  flutter_local_notifications: ^17.1.0
  path_provider: ^2.1.2
  connectivity_plus: ^6.0.0
  table_calendar: ^3.1.3
  flutter_widget_from_html: ^0.17.2
  flutter_animate: ^4.5.2
  animations: ^2.1.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
